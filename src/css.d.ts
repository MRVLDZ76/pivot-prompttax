// Ambient declarations for side-effect CSS imports (e.g. `import './globals.css'`).
// Keeps the TypeScript language server from flagging global stylesheet imports
// that Next.js resolves at build time.
declare module '*.css'
